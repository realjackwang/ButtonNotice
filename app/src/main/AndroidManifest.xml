<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.notice.button.button">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="纽扣通知"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Fragment.MainActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Button.buttonLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Notice.noticeMain" />
        <activity android:name=".User.userInfo" />
        <activity android:name=".Notice.noticeDetialFile" />
        <activity android:name=".Notice.noticeNew" />
        <activity android:name=".Notice.noticeDetailActivity"></activity>
    </application>

</manifest>